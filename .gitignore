# Python
__pycache__/
*.pyc
.venv/
venv/
venv_train/

# macOS
.DS_Store

# Datasets copied from HF / GitHub (35 MB; regenerate per docs/overview.md)
data/

# Experiment outputs pulled from the cluster (GBs of transcripts, rollout logs, adapters, embeddings).
# `results` is a symlink to ~/code_nobackup/contract/results, outside Dropbox; the probe's small text
# summaries live in docs/probe_summaries/ so they stay in the repo.
results

# Local scratch
*.log
*.err
